<!-- Main -->
<main class="wrapper">

    {{>sidebar}}

    <!-- Content -->
    <section class="content-wrapper" id="dashboard">

        {{>topbar}}

        {{#if properties}}
        {{else}}
        <!-- Main Content -->
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12 p-0">
                        <div class="p-4 text-center">
                            <span class="message">There are no properties registered under your account.</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        {{/if}}

    </section>

</main>